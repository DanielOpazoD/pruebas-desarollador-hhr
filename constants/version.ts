/**
 * Schema Versioning Configuration
 * 
 * Each major change to the DailyRecord structure should increment this version.
 * This prevents older versions of the app from overwriting data in a newer format.
 */
export const CURRENT_SCHEMA_VERSION = 1;
